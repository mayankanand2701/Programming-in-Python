result=a+b

# TypeError:
# This exception occurs if the operands (variables a and b in this case) are of incompatible types for addition.
# For example,trying to add a string to an integer or performing an unsupported operation.
# a = 5
# b = "Hello"
# result = a + b  # Raises TypeError


# NameError:
# This exception occurs if either a or b is not defined before using them in the addition operation.
# Ensure that both variables are assigned values before attempting the addition.
# result = a + b  # Raises NameError if either a or b is not defined
